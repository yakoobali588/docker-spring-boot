apiVersion: v1
kind: Service
metadata:
  name: {{ include "may-26-chart.fullname" . }}
  labels:
    {{- include "may-26-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "may-26-chart.selectorLabels" . | nindent 4 }}
